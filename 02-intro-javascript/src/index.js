// file of work

const active = true;

// let msg = '';
//
// if (active) {
//   msg = 'active';
// } else {
//   msg = 'Unactive';
// }

//const msg = active ? 'Active' : 'unactive';
//const msg = active ? 'Active' : null;

//const msg = active && 'Active';
const msg = active && 'Active';
console.log(msg);
